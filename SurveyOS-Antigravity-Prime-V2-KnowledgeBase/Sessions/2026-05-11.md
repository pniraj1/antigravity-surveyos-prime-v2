---
date: 2026-05-11
tags: surveying, session-log, ui-update
branch: master
---

> [!info] Session Summary
> Restored the drag-and-drop row reordering feature in the Parts Assessment Grid.

## What We Did

1. Restored `dnd-kit` implementation in `src/components/claim/AssessmentGrid.tsx` for row reordering.
2. Brought back `SortableRow` component with `useSortable` hook for row-level physics and dragging.
3. Hooked up the sorting context and `onDragEnd` listener to call `reorderAssessmentRows` in the claim store.
4. Brought back the `GripVertical` drag handle column.

## Files Changed

| File | Action | Notes |
| --- | --- | --- |
| `src/components/claim/AssessmentGrid.tsx` | Modified | Restored `dnd-kit` contexts, sensors, and `SortableRow` |

## Key Decisions

> [!tip] Key Takeaways
> - Matched the exact structure from a previous working version (`old_grid_utf8.tsx`) to ensure no regressions in column sorting and layout.
> - Preserved the ability to toggle visibility of optional columns and navigate via arrow keys.

## Open / Next Session

- [ ] Further test drag-and-drop on mobile or touch environments to ensure sensors are responding optimally.
